{% extends 'base.html.twig' %}
{% block title %}Edit Discussion{% endblock %}
{% block body %}
<h1>Edit Discussion (Admin)</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Save</button>
{{ form_end(form) }}
<a href="{{ path('admin_discussion_dashboard') }}">Back to dashboard</a>
{% endblock %}
