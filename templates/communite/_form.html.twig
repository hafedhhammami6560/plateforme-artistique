{{ form_start(form) }}
    <div>
        {{ form_row(form.name) }}
    </div>
    <div>
        {{ form_row(form.description) }}
    </div>
    <button type="submit">Enregistrer</button>
{{ form_end(form) }}


