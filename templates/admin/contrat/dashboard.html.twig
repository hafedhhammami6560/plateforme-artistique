{% extends 'base.html.twig' %}

{% block title %}Dashboard - Contrats{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1><i class="fas fa-file-contract"></i> Contrats Dashboard</h1>
        <a href="#" class="btn btn-primary" style="background-color: #05468b; border-color: #05468b;">
            <i class="fas fa-plus"></i> Ajouter un contrat
        </a>
    </div>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="fas fa-check-circle"></i> {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="fas fa-exclamation-circle"></i> {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    <div class="card shadow-sm">
        <div class="card-body">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Numéro</th>
                        <th>Artiste / Producteur</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                {% for contrat in contrats %}
                    <tr>
                        <td>{{ contrat.id }}</td>
                        <td>{{ contrat.numeroContrat }}</td>
                        <td>{{ contrat.artiste.name }} / {{ contrat.producteur.name }}</td>
                        <td>
                            <a href="{{ path('admin_contrat_show', {'id': contrat.id}) }}" class="btn btn-sm btn-info">
                                <i class="fas fa-eye"></i> Voir
                            </a>
                            <a href="{{ path('admin_contrat_edit', {'id': contrat.id}) }}" class="btn btn-sm btn-warning">
                                <i class="fas fa-edit"></i> Modifier
                            </a>
                            <form method="post" action="{{ path('admin_contrat_delete', {'id': contrat.id}) }}" style="display:inline" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce contrat ?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ contrat.id) }}">
                                <button class="btn btn-sm btn-danger">
                                    <i class="fas fa-trash"></i> Supprimer
                                </button>
                            </form>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
