{% extends 'base.html.twig' %}
{% block title %}Edit Contrat{% endblock %}
{% block body %}
<h1>Edit Contrat (Admin)</h1>
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Save</button>
{{ form_end(form) }}
<a href="{{ path('admin_contrat_dashboard') }}">Back to dashboard</a>
{% endblock %}
