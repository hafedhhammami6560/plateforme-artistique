{{ form_start(form) }}
    <div>
        {{ form_row(form.name) }}
    </div>
    <div>
        {{ form_row(form.address) }}
    </div>
    <div>
        {{ form_row(form.email) }}
    </div>
    <div>
        {{ form_row(form.communite) }}
    </div>
    <button type="submit">Enregistrer</button>
{{ form_end(form) }}


